let names = ["Abdurashid", "Abdurahmon", "Madina", "Gulchehra", "Javlonbek"]

for (let i = 0; i < names.length; i++) {
    if (names[i].startsWith('Abdu')) {
        console.log(names[i])
    }
    if (names[i].startsWith('Gul')) {
        console.log(names[i])
    }
}